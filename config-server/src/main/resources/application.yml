server:
  port: 9091

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/toduchieu/config-server.git
          default-label: main
        bootstrap: false